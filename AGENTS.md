# AGENTS.md

READ ME 프로젝트에서 AI 에이전트(Claude Code, GPT Codex 등)가 작업할 때 지켜야 할 규칙입니다.

## 1. 언어

- 답변, 코드 설명, 커밋 메시지, 주석은 **한국어**로 작성합니다.
- UI 문구도 한국어를 기본으로 하며, 영문은 `eyebrow` 라벨(`ABOUT READ ME`, `MEMBER LOGIN` 등)처럼 의도된 위치에만 사용합니다.

## 2. 리미 색상 팔레트

`app/globals.css`의 `:root` 토큰을 벗어나지 않습니다. 새로운 색상 값을 임의로 추가하지 마세요.

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--cream` | `#f7f3e7` | 기본 배경 |
| `--sand` | `#eadfc6` | 보조 강조, 어두운 배경 위 텍스트 |
| `--sage` | `#9bae8f` | 섹션 배경, 아이콘 |
| `--forest` | `#5b6b57` | 주요 강조, 링크, 아이콘 |
| `--ink` | `#3b3a34` | 본문 텍스트, 어두운 섹션 배경 |
| `--paper` | `#fffdf7` | 카드 배경 |
| `--muted` | `#716f65` | 보조 텍스트 |
| `--line` | `rgba(59, 58, 52, .14)` | 테두리, 구분선 |

- 색상은 항상 `var(--forest)` 형태로 참조하고 하드코딩하지 않습니다.
- 투명도가 필요하면 기존 톤(`rgba(247,243,231,.62)` 등)의 규칙을 따릅니다.

## 3. 모바일 화면 확인

- 모든 변경은 데스크톱과 모바일을 함께 확인합니다.
- 브레이크포인트는 `app/globals.css`에 정의된 **820px**(태블릿·모바일)과 **430px**(소형 모바일)을 사용합니다.
- 새 섹션을 추가하면 두 브레이크포인트의 미디어 쿼리도 함께 갱신합니다.
- 가로 스크롤이 생기지 않는지, 그리드가 1열로 접히는지 확인합니다.

## 4. Git 작업

- **사용자 승인 없이 `git commit`, `git push`를 실행하지 않습니다.**
- 커밋 메시지 작성 요청은 "메시지를 작성하라"는 의미이며, 커밋 실행 요청이 아닙니다.
- `git reset --hard`, `git push --force` 등 되돌리기 어려운 명령은 명시적 지시가 있을 때만 사용합니다.

## 5. 검증 명령

파일을 수정한 뒤에는 아래를 실행해 통과를 확인합니다.

```bash
npm run lint
npm run build
```

- 실패하면 원인을 수정한 뒤 다시 실행합니다. 실패 상태로 작업을 종료하지 않습니다.
- 실행하지 못한 경우에는 그 사실을 명확히 보고합니다.

## 6. 디자인 참고 원칙

- 기존 페이지의 구조와 톤을 참고하되 **그대로 복제하지 않습니다.**
- 공통 패턴(`.section`, `.section-shell`, `.button`, `.eyebrow` 등)은 재사용하고, 콘텐츠와 레이아웃은 해당 페이지의 목적에 맞게 새로 설계합니다.
- 반복되는 마크업은 컴포넌트로 분리합니다(`components/` 참고).

## 7. 영역 분리

라우트별 역할을 명확히 구분하고 섞지 않습니다.

| 영역 | 경로 | 규칙 |
| --- | --- | --- |
| 공개 | `/`, `/about`, `/meeting` | 비로그인 방문자 대상. 회원 전용 정보 노출 금지 |
| 인증 | `/login`, `/signup` | 로그인·가입 진입점 |
| 회원 | `/my` | 회원 전용. 참여 기수, 질문, 기록 |
| 운영자 | `/admin` | 운영 전용. **공개 내비게이션에 노출하지 않음** |

- 실서비스 전환 시 `/my`와 `/admin`은 미들웨어와 서버 측 권한 검사를 반드시 거칩니다.
- 관리자 기능을 회원 화면에 섞지 않습니다.

## 8. 커밋 메시지 형식

```
TYPE. 한국어 요약 (50자 이내, 끝에 마침표 없음)

<빈 줄>
- 변경 내용을 불릿으로 정리
- 파일이나 영역 단위로 묶어 작성
```

- **첫 단어(TYPE)는 항상 대문자**입니다: `ADD`, `FEAT`, `FIXED`, `DELETE`, `UPDATE`, `REFACTOR`, `STYLE`, `DOCS`, `CHORE`
- **TYPE 뒤에는 콜론(`:`)이 아니라 마침표(`.`)와 공백 하나를 둡니다.** 예: `FEAT. ...` (O) / `FEAT: ...` (X)
- 요약은 한국어 명사형 또는 서술형으로 통일합니다.
- 본문 불릿은 "무엇을 왜 바꿨는지" 위주로 작성합니다.

예시:

```
FEAT. 리미 브랜드 아이덴티티 적용 및 사용자 여정 개편

- globals.css에 브랜드 토큰과 공통 섹션 클래스 정의
- 랜딩 페이지를 히어로·여정·FAQ·CTA 구조로 재구성
- RemiGuide, Footer 컴포넌트 신규 추가
```
