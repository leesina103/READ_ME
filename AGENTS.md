# AGENTS.md

READ ME 프로젝트에서 AI 에이전트가 지켜야 할 규칙입니다.

## 1. 언어

- 답변, 코드 설명, 커밋 메시지, 주석은 한국어로 작성합니다.
- UI 문구도 한국어를 기본으로 하며, 영문은 `eyebrow` 라벨처럼 의도된 위치에만 사용합니다.

## 2. 디자인

- 색상은 `app/globals.css`의 `:root` 토큰(`--cream`, `--sand`, `--sage`, `--forest`, `--ink`, `--paper`, `--muted`, `--line`)만 사용합니다.
- 색상값을 하드코딩하지 않고 `var(--forest)`처럼 참조합니다. 투명도는 기존 CSS의 톤을 따릅니다.
- 기존 페이지를 그대로 복제하지 않고 공통 패턴(`.section`, `.section-shell`, `.button`, `.eyebrow`)을 재사용합니다.
- 반복되는 마크업은 `components/`의 컴포넌트로 분리합니다.

## 3. 반응형

- 데스크톱과 모바일을 함께 확인합니다.
- `app/globals.css`의 820px과 430px 브레이크포인트를 사용합니다.
- 새 섹션은 두 브레이크포인트에 대응하고, 가로 스크롤과 1열 전환 여부를 확인합니다.

## 4. 영역 분리

| 영역 | 경로 | 규칙 |
| --- | --- | --- |
| 공개 | `/`, `/about`, `/meeting` | 회원 전용 정보 노출 금지 |
| 인증 | `/login`, `/signup` | 로그인·가입 진입점 |
| 회원 | `/my` | 참여 기수, 질문, 기록 등 회원 전용 |
| 운영자 | `/admin` | 공개 내비게이션에 노출하지 않음 |

- 관리자 기능을 회원 화면에 섞지 않습니다.
- 실서비스의 `/my`와 `/admin`에는 미들웨어와 서버 측 권한 검사를 적용합니다.

## 5. Git

- 커밋에는 서로 관련된 변경만 포함합니다.
- 커밋 전에 변경 내용을 확인하고 사용할 커밋 메시지를 사용자에게 보여줍니다.
- 사용자 승인 없이 `git commit`이나 `git push`를 실행하지 않습니다. 커밋 메시지 작성 요청은 실행 승인이 아닙니다.
- 커밋 후 커밋 해시와 작업 트리 상태를 확인합니다.
- `git reset --hard`, `git push --force` 등 되돌리기 어려운 명령은 명시적 지시가 있을 때만 실행합니다.

커밋 메시지는 다음 형식을 사용합니다.

```text
TYPE. 한국어 요약

- 무엇을 왜 변경했는지 설명
```

- `TYPE`은 `ADD`, `FEAT`, `FIXED`, `DELETE`, `UPDATE`, `REFACTOR`, `STYLE`, `DOCS`, `CHORE` 중 하나를 사용합니다.
- `TYPE` 뒤에는 콜론이 아닌 마침표와 공백을 넣습니다.
- 요약은 50자 이내로 작성하고 끝에 마침표를 붙이지 않습니다.
- 본문은 필요한 경우에만 파일이나 영역 단위로 작성합니다.

## 6. 검증

파일 수정 후 다음 명령을 통과시킵니다.

```bash
npm run lint
npm run build
```

실패하면 원인을 수정하고 다시 실행합니다. 실행하지 못한 경우에는 그 사실을 보고합니다.

## 7. 이 PC의 함정

모르고 시작하면 반드시 걸리는 것들입니다.

**`python3`는 실행되지 않습니다.** Windows 앱 실행 별칭 스텁이라 `python3 - <<EOF` 형태로 부르면 "Python"만 출력하고 아무 일도 하지 않습니다. 오류도 없어서 수정이 적용된 줄 착각하기 쉽습니다. 여러 파일 문자열 치환은 `.mjs` 스크립트를 써서 `node`로 실행하고, 치환 뒤에는 `grep`으로 옛 문구가 남아 있지 않은지 반드시 확인합니다.

**이미지 가공은 `sharp`로 합니다.** ImageMagick과 python은 없습니다. 프로젝트 밖 스크립트에서는 `createRequire("C:/Workspace/READ_ME/package.json")("sharp")`로 불러옵니다. 일반 `import sharp from "sharp"`는 모듈을 찾지 못합니다. 다만 **리미 일러스트(`public/theme-remi-*.png`)는 색과 밝기를 건드리지 않습니다.** 새 표지가 필요하면 장면이 맞는 원본을 그대로 씁니다. 꼭 가공해야 하면 `tint()`는 흑백화 후 색을 얹어 초록이 죽으니 `modulate` + 반투명 SVG `composite`로 합니다.

**dev 서버는 포트 3000에 단 하나만 띄웁니다.** 3000이 막혀 있으면 다른 포트로 피하지 말고 점유 프로세스를 정리한 뒤 3000으로 띄웁니다. 사용자가 `http://localhost:3000/...`을 직접 입력해 확인하기 때문입니다.

Next 15.5는 dev 잠금 파일이 없어 같은 폴더에서 두 서버가 동시에 뜹니다. 2026-09-14 로그인 불가 사건이 여기서 나왔습니다. 유령 `next dev` 두 개(3000, 3001)가 같은 `.next`를 공유했고, 3001이 `/login`을 컴파일하며 `app-paths-manifest.json`을 자기 라우트만 담아 덮어써서 3000 서버가 `/my`와 서버 액션을 찾지 못했습니다.

서버를 띄우기 전 순서:

1. `Get-NetTCPConnection -State Listen | ? { $_.LocalPort -ge 3000 -and $_.LocalPort -le 3010 }` 과 `Get-CimInstance Win32_Process -Filter "Name='node.exe'"` 로 이 프로젝트의 `next dev`를 찾습니다.
2. 사용자가 띄운 기록이 없으면 유령입니다. npm 래퍼·next 래퍼·자식(`start-server.js`) 세 프로세스를 모두 종료합니다. `OpenAI\Codex\runtimes` 아래 node.exe는 우리 서버가 아니니 건드리지 않습니다.
3. 유령이 하나라도 있었으면 `.next`를 삭제하고 시작합니다. 매니페스트가 오염돼 있습니다.
4. 검증: 3000 하나만 LISTEN, `/login` 200, `/my` → 307 `/login?next=%2Fmy`.

**6장의 `npm run build`는 dev 서버를 끄고 실행합니다.** dev 서버가 도는 중에 빌드하면 `.next`를 덮어써 CSS가 안 붙습니다. 검증이 끝나면 서버를 다시 3000으로 띄워둡니다.

## 8. 이미 정해진 것

사용자가 이유를 따져 내린 결정입니다. 되돌리는 제안을 다시 하지 않습니다.

**출생연도만 받습니다.** 생년월일 전체가 아니라 연도만, 위치는 회원가입이나 인터뷰 예약이 아닌 **가입 신청서(`/membership/apply/N`)**, 필수 항목입니다. 목적이 "기수 연령 구성 파악"이라 최소수집 원칙상 연도면 충분합니다. PASS 같은 본인인증도, "만 19세 이상" 자기확인 문구도 넣지 않습니다. 인터뷰에서 직접 만나기 때문입니다. 출생연도를 프로필까지 흘려보내는 건 기수 페이지에 연령 구성 표시가 필요해질 때만 합니다.

**멤버 소개는 현재 기수 동료들에게만 공개합니다.** `/membership/members`와 현재 기수의 첫 만남 페이지에서 나를 표현하는 단어, 자기소개와 동료들에게 하고 싶은 말을 서로 봅니다. 요청자와 동료의 `profiles.cohort`가 같아야 하며, 지난 참여 이력만으로는 소개를 볼 수 없습니다. **회원이 끄고 켜는 스위치는 두지 않습니다.** 보여주기 싫으면 `/my`에서 내용을 짧게 고칠 수 있습니다. 공개 스위치나 전체 회원 공개를 다시 제안하지 않습니다. 새 공유 항목이 생기면 입력란 라벨에도 "현재 기수 동료들에게만 보여요."처럼 공개 사실을 함께 적습니다. 지난 기수의 1~8주차 대화방 읽기 권한은 그대로 유지합니다.

**파티 초대장은 링크 전용입니다.** `/membership/party/[기수]`는 멤버십 홈 카드에도, 좌측 메뉴에도 링크를 두지 않습니다. 운영자가 초대할 사람에게 링크를 직접 보냅니다. **링크가 없는 것은 누락이 아니라 의도입니다.** 다만 현재 접근 제한은 "활성 회원 + 같은 기수"뿐이라 URL을 아는 같은 기수 회원은 볼 수 있습니다. 진짜 링크 전용으로 만들려면 추측 불가능한 슬러그나 토큰이 필요합니다.

**지난 기수 대화방은 읽기 전용입니다.** 두 기수 대화방을 모두 보되 지난 기수에는 답변을 남길 수 없습니다. 답변을 남기지 않았던 주차는 동료 답변이 계속 잠긴 상태로 둡니다. 사용자가 이 점을 알고 선택했습니다. **멤버십 만료(paused·expired) 개념은 현재 운영에 없습니다.**

## 9. 기수와 회원 데이터

- `profiles.cohort`는 **현재 기수**입니다. 참여 이력은 `member_cohorts`(user_id, cohort) 표에 쌓이며, 프로필 기수가 바뀌면 트리거가 한 줄 추가합니다.
- `cohorts.starts_at`은 온라인 대화방 1주차 공개 시각입니다. 매주 같은 요일·시각에 다음 주차가 열립니다. `cohorts.ends_at`은 종료일이고 null이면 진행 중입니다. 종료일이 지난 기수는 현재 기수라도 읽기 전용입니다.
- 조회 정책은 `is_cohort_member(cohort)`로 참여했던 기수까지 허용하고, 답변 저장은 현재 기수에만 가능합니다(`save_session_answer`가 `cohort_ended`로 막습니다).
- **`profiles` 표는 본인 행만 조회하는 잠금을 유지합니다.** 공유가 필요한 항목은 `list_member_directory()` 함수로만 내보냅니다. 새 공유 항목이 생겨도 표의 정책을 푸는 방식으로 풀지 않습니다.
- 재참여 회원(1기 → 2기 재신청)은 `review_membership_application`이 처리합니다. 이미 `claimed_by`가 찍힌 invitation을 만나면 이름·기수를 새 신청서 값으로 갱신합니다. `member_invitations_normalized_email_key`는 여전히 이메일 전역 unique입니다.
- 기수 시작일·종료일·신청 접수 여부는 `/admin/cohorts`에서 관리하며 한국 시간 기준 시작일 0시·종료일 자정으로 저장합니다.
- **새 기수를 열 때는 `data/cohortThemes.ts`에 기수 주제를 한 줄 추가해야** 지난 기수 주차 목록이 그 기수 주제로 나옵니다.
- 파티 초대장 내용은 `data/partyInvitations.ts`에 기수별로 적습니다. 운영자 화면과 DB가 아직 없어 여기에 직접 씁니다. 나중에 DB로 옮길 때 `PartyInvitation` 모양만 지키면 페이지는 그대로 씁니다.

## 10. 오프라인 발제문

노크노크 대화록 형식을 참고한 READ ME 판을 만드는 중입니다. A4 7장 순서는 표지(주제 질문 + BOOK/AUTHOR/DATE/HOST 기입) → 01 RULE → 02 BOOK GUIDE(출처 칸 포함) → 03 DISCUSSION CANVAS(질문당 1장) → 04 NEXT QUESTION → 05 READ ME TASTE(본인 소개 / 참여 소감 / 익명 포스트잇 질문)입니다.

**발제 질문마다 `토의 전 내가 생각한 답 / 오늘 새로 들은 관점 / 다음 주에 살펴볼 행동이나 질문` 세 칸을 두는 것이 핵심입니다.** 다른 독서모임과의 가장 큰 차별점이고, READ ME가 사전 질문 → 오프라인 토의 → 사후 실천·기록으로 소개하는 흐름을 종이에서도 그대로 보여줍니다. 발제문이나 기록 화면을 만들 때 이 세 칸 흐름을 유지합니다.

인쇄물 색과 서체는 웹과 같은 토큰(크림·세이지·포레스트, Noto Serif/Sans KR)을 쓰고, 캐릭터는 리미 원화를 색·밝기 건드리지 않고 그대로 잘라 씁니다(7장 참고).

초안은 `docs/발제문/`에 있습니다. **이 폴더는 저장소를 무겁게 하지 않으려고 git에 넣지 않았습니다**(HTML + PDF 17MB). clone만 한 환경에는 없으니, 파일이 보이지 않으면 사용자에게 물어봅니다. HTML→PDF는 Chrome을 CDP(`--remote-debugging-port` + `Page.printToPDF`, `preferCSSPageSize: true`)로 붙여 A4로 뽑고, 그림은 base64로 넣어 단일 파일로 만듭니다.
